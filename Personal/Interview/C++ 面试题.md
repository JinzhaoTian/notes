1. 重载、隐藏、重写（覆盖）三者的区别？
   在同一作用域中，同名函数的形式参数（指参数的个数、类型或者顺序）不同时，构成函数重载。
   函数隐藏指不同作用域中定义的同名函数构成函数隐藏（不要求函数返回值和函数参数类型相同）。
   派生类中与基类同返回值类型、同名和同参数的**虚函数重定义**，构成虚函数覆盖，也叫虚函数重写。
2. new/delete与malloc/free 的区别
   malloc/free是C++/C语言的标准库函数，而new/delete是C++的运算符，所以对于非内部数据类型的对象而言，光用malloc/free是无法满足动态对象的要求的。 对象在创建的同时需要自动执行构造函数，对象在消亡之前必须执行析构函数。 对于类，new 和delete会调用构造，析构函数。new，delete都是能感知到类型的。new返回一个制定的类型，delete删除一个指定的类型，从而不用给定size。 而malloc与free都是处理void类型的。用时时必须经过强制类型转换。
3. 什么是多态机制？多态底层实现是怎么样的？
   当我们使用一个基类类型的引用或者指针，调用一个虚函数时就引发动态绑定/多态的发生。函数运行版本由传入的实参类型决定。可以用父类型的指针指向其子类的实例，然后通过父类的指针调用实际子类的成员函数。 当我们使用基类的引用、指针去调用基类中定义的一个虚函数时，并不知道该函数真正作用的对象是什么类型，可能是一个基类对象，也可能是一个派生类对象，这一切直到运行时才可知道。所以多态又称运行时绑定/动态绑定。
4. 父类构造函数中是否可以调用虚函数？
   在父类的构造函数中调用虚函数是可以的，但是不会实现多态。在定义子类对象的时候，会先调用父类的构造函数，而此时虚函数表以及子类函数还没有被初始化，为了避免调用到未初始化的内存，c++标准规范中规定了在构造子类时调用父类的构造函数，而父类的构造函数中又调用了虚成员函数，即使这个虚成员函数被子类重写，也不允许发生多态的行为，所以使用的是静态绑定，调用了父类的函数。
5. 析构函数可以是虚函数吗？为什么？
   析构函数可以是虚函数，但是构造函数不可以是虚函数。如果析构函数不是虚函数，那么通过父类指针释放子类对象时，子类的析构函数可能调不到，导致内存泄漏。
6. 如果析构函数不是虚函数，一定会出现内存泄露吗？
   不一定会出现内存泄漏，如果通过子类指针释放子类对象时，就可以顺利的调用到子类的析构函数和父类的析构函数。 
7. 创建10个实例有几个虚函数表？
   虚函数表的数量不会根据创建几个实例而改变，如果一个类有一个虚函数表，那么创建10个实例之后也是一个虚函数表，如果有两个虚函数表也还是两个。虚函数表的数量和继承的基类数量有关，虚函数表指针的数量和实例的数量有关。 
8. 指针和引用的区别是什么？
   相同点：两者都是地址的概念，指针指向一块内存，其内容为所指内存的地址；引用是某块内存的别名。 不同点：
	   1. 指针是一个实体，而引用仅是个别名；
	   2. 引用使用时无需解引用(\*)，指针需要解引用；
	   3. 引用只能在定义时被初始化一次，之后不可变；指针可变；
	   4. 引用没有 const，指针有 const；const修饰的指针不可变；
	   5. 引用不能为空，指针可以为空；
	   6. “sizeof 引用”得到的是所指向的变量(对象)的大小，而“sizeof 指针”得到的是指针本身(所指向的变量或对象的地址)的大小；
	   7. 指针和引用的自增(++)运算意义不一样；
9. 讲一下vector的扩容机制，实现原理，内存是怎么分配的？ 
   vector中文名字是动态数组， 其内部数据结构就是一个数组， 但是在数组元素不够用的时候，就要动态的重新分配， 一般是现在大小的两倍， 然后把原数组的内容拷贝过去。所以， 在一般情况下， 其访问速度同一般数组， 只有在重新分配发生时， 其性能才会下降。 
10. vector如何快速删除内部对象？
    vector本身自带的删除函数有erase，pop_back，clear。如果要删除vector中指定的元素，则可以使用 erase函数，但是该函数效率会很低。C++ <algorithm>中提供了一个remove方法。
11. vector会自动减容嘛？
    在C++标准库容器vector的容量是不会自动的缩减的，如果想要减容可以用`std::vector\<T\>(v1).swap(v2);`
12. 说一说map的底层实现？
    
13. 为什么map底层要用红黑树实现而不用AVL实现？
    
14. C++的那些容器都是线程安全的还是非线程安全的？ 
    STL容器不是线程安全的。加锁是一种解决方案。
15. 说一下你用过哪些C++11新特性
16. 左值和右值分别是什么？移动语义和完美转发是什么？
    移动语义的引入是为了解决在进行大数据复制的时候，将动态申请的内存空间的所有权直接转让出去，不用进行大量的数据移动，既节省空间又提高效率。 完美转发指可以写一个接受任意实参的函数模板，并转发到其它函数，目标函数会收到与转发函数完全相同的实参，转发函数实参是左值那目标函数实参也是左值，转发函数实参是右值那目标函数实参也是右值。可以使用`std::forward()`。
17. 智能指针有几种？分别介绍一下他们的底层实现？
18. 说一下lambda表达式的底层实现 
    编译器为我们了创建了一个类，这个类重载了()，让我们可以像调用函数一样使用。
19. lambda表达式（匿名函数）的主要作用是什么？它的参数传递时内存是怎样布局的？
20. static关键字
21. const关键字， 常量指针和指针常量，顶层const和底层const分别是什么？ 
    常量指针：`const int* pa;` 和 `int const* pb;`，指向可以改，但是不能通过该指针改变所指向的值，也被称作底层const，指针所指向的值是个常量。 
    指针常量：`int* const pc;`，指向不可以改，但可以通过该指针改变所指向的值，被称作顶层const，因为指针本身是个常量。 常量指针常量：`int const* const pd;` 不可以修改指向，也不可以修改所指向的值。
22. 说一下深浅拷贝
23. 什么是抽象类？
24. C++的空类会默认生成哪几个成员函数？
    对于空类，编译器不会生成任何的成员函数，只会生成1个字节的占位符。 编译器只会在需要的时候生成6个成员函数：一个缺省的构造函数、一个拷贝构造函数、一个析构函数、一个赋值运算符、一对缺省取址运算符和一个缺省取址运算符 const。
25. （拷贝）赋值函数是什么？
26. 什么情况下必须使用初始化列表初始化数据成员？ 
    需要初始化的数据成员是对象的情况；需要初始化const修饰的类成员；需要初始化引用成员数据。
27. 内存是怎样分配的？
28. 讲一下四种类型转换符？
    static_case<type-id>（静态转换）：基本数据类型之间转换，有类型指针与 void * 之间转换（有类型指针之间转换不行），基类和派生类之间指针或引用的转换。 
    dynamic_cast<type-id>（动态转换）：用于将一个父类的指针/引用转化为子类的指针/引用（下行转换）。
    const_cast<type-id>（常量转换）：常量指针（或引用）与非常量指针（或引用）之间的转换。 
    reinterpret_cast<type-id>（不相关类型的转换）：用在任意指针（或引用）类型之间的转换，能够将整型转换为指针，也可以把指针转换为整型或数组。高危操作。
29. C和C++的区别？

30. inline和define的区别？ 
    C++提供了inline函数，以减少函数调用的开销。内联函数是在调用时在行中扩展的函数。调用内联函数时，将在内联函数调用时插入或替换内联函数的整个代码。替换由C++编译器在编译时执行。如果内联函数很小，则可以提高效率。 inline函数看起来像函数，动作像函数，但是又比宏方便很多，调用inline函数省去了参数压栈、生成汇编语言的 CALL调用、返回参数、执行return等过程所花费的额外开销。不过inline函数也有缺点：增加目标码的大小。因为inline函数的设计思想是对该函数的所有调用都用函数本身替换掉，和宏定义一样，但是比宏定义用起来要简单很多。而且inline的调试同样很难。
	1. 宏是在预编译阶段简单文本替代， inline在编译阶段实现展开
	2. 宏肯定会被替代，而复杂的inline函数不会被展开
	3. 宏容易出错（运算顺序），且难以被调试，inline不会
	4. 宏不是类型安全，而inline是类型安全的，会提供参数与返回值的类型检查
	5. 当出现以下情况时inline失败
        1. 函数size太大
        2. inline虚函数
        3. 函数中存在循环或递归
        4. 函数调用其他inline函数
31. C++中struct和class有什么区别？ 
    struct和class的最大区别是，默认访问权限。class默认的是private，strcut默认的是public。
32. class类的内存是怎样分布的？
33. 大端字节序和小端字节序解释一下？ 
    Big-Endian就是高位字节排放在内存的低地址端，低位字节排放在内存的高地址端；Little-Endian就是低位字节排放在内存的低地址端，高位字节排放在内存的高地址端。 可以用类型转换加解引用判断。
34. 内存溢出和内存泄露有什么区别？ 
    内存溢出就是要求分配的内存超出了系统所给的。内存泄漏是指向系统申请分配内存进行使用（new），但是用完后不归还（delete），导致占用有效内存。
35. 一个类如何访问另一个类的private函数？ 
    设置友元类
36. C++的多线程与多进程 
    C++的多线程与多进程同属于并发技术。多进程，Linux下创建子进程的调用是fork()。进程是程序在计算机上的一次执行活动，是系统分配资源的基本单位。 线程是可执行代码的可分派单位。在基于线程的多任务的环境中，所有进程有至少一个线程，但是它们可以具有多个任务。这意味着单个程序可以并发执行两个或者多个任务。linux提供的多线程的系统调用是pthread_create()。 多进程和多线程的区别，在于前者开销大，后者开销较小。Linux下不管是多线程编程还是多进程编程，最终都是用do_fork实现的多进程编程，只是进程创建时的参数不同，从而导致有不同的共享环境。Linux线程在核内是以轻量级进程的形式存在的，拥有独立的进程表项，而所有的创建、同步、删除等操作都在核外pthread库中进行。
37. C++菱形继承与虚继承 
    简单来说，菱形继承就是class B继承class A，class C继承class A，然后class D继承class B和class C，这样的话class D中就有两份class A的拷贝了，并且此时访问class A的时候就会出现二义性。 虚继承是一种机制，类通过虚继承指出它希望共享虚基类的状态。对给定的虚基类，无论该类在派生层次中作为虚基类出现多少次，只继承一个共享的基类子对象，共享基类子对象称为虚基类。虚基类用virtual声明继承关系就行了。如果class B虚继承class A，class C也虚继承class A，那么class D里面就会只出现一份class A的拷贝了，解决了菱形继承中派生类多个基类内存拷贝的问题，并没有解决多重继承的二义性问题。
38. Overload, Overwrite, Override 各自的特点和意义
    Overload: 函数重载(名字相同，参数不同) 
    Overwrite：覆盖 
    Override: 虚函数重载
39. C++中static_cast 和 dynamic_cast 的区别
    static_cast可以显式的做一些自动转换，如一些int, char一些基础类型的转换，以及指针之间的转换。但是其不保证安全性。
    dynamic_cast主要作用其实在于把一个基类指针转化为子类指针，因为这个基类指针真正指向的不一定是我们想转换的类型的对象，所以转换可能失败，dynamic_cast能够知道失败而返回NULL，而static_cast就没那么聪明了，原因是dynamic_cast会利用rtti去查找该转换是否可行。