
数据库的本体是 SQL Server，其配套的管理工具是 SQL Server Management Studio ，后者并不是必须的，因为一切管理操作都可以通过 SQL 语句完成。

### 服务器身份验证模式

安装过程中， SQL Server 数据库引擎 设置为 “Windows 身份验证模式” 或 “SQL Server 和 Windows 身份验证模式” 。 安装完成后，可以随时更改身份验证模式：

- **Windows 身份验证模式**：如果在安装过程中选择了“Windows 身份验证模式”，则 sa 登录名将被禁用，安装程序会分配一个密码。 如果稍后将身份验证模式更改为“SQL Server 和 Windows 身份验证模式”，则 sa 登录名仍处于禁用状态。 若要使用 sa 登录名，请使用 ALTER LOGIN 语句启用 sa 登录名并分配一个新密码。 sa 登录名只能使用 SQL Server 身份验证连接到服务器。

```sql
ALTER LOGIN sa ENABLE;
GO
ALTER LOGIN sa WITH PASSWORD = '<enterStrongPasswordHere>';
GO
```




连接不上问题，要打开TCP/IP：
[node.js - NestJS, Typeorm, SQL Server - failed to connect to localhost:1433 - Could not connect (sequence) - Stack Overflow](https://stackoverflow.com/questions/73160650/nestjs-typeorm-sql-server-failed-to-connect-to-localhost1433-could-not-co)
![](_imgs/Pasted%20image%2020240319175208.png)
