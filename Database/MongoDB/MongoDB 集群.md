

### 分片集群

三种分片方式：

1. 基于数据范围：比如按照年龄的划分，但是容易造成数据分布不均衡，导致热点片的情况。
2. 基于Hash：通过hash散列到不同的片区，解决了数据不均匀的情况。但是范围查询的场景效率特别低，需要遍历全部的分片才能满足业务查询。
3. 基于Zone：按照地域去划分，因其地域性则可以天然的按地域去划分分片。

数据均衡，Mongo提供两个程序进行数据均衡：切割器和平衡器。切割器可以对某个源分片的数据按chunk（数据逻辑块）去做切割。平衡器在数据不均匀的情况下使用，去需要移动的分片上去做数据切割，再把数据移动到数据少的分片上。
