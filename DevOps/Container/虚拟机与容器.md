虚拟机（VM）运行完整的操作系统（包括自己的内核），
![](_imgs/Pasted%20image%2020251215101630.png)
容器在主机操作系统的内核上运行，与主机的用户模式环境隔离，并不能对其进行自由访问， 而只能获取系统的隔离视图，在某些情况下只能获取其虚拟化视图。
![](_imgs/Pasted%20image%2020251215101547.png)

容器映像由一系列层组成，每个层包含一组文件，这些文件重叠在一起时表示容器映像。由于容器的分层性质，你不必始终以基础映像为目标来生成容器。相反，可以将另一个已承载所需框架的映像作为目标。
