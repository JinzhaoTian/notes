调度是指决定哪个任务在何时由哪个执行单元运行的过程。

### 主要类型

1. **操作系统级调度**：
    - 决定哪个进程/线程获得 CPU 时间
    - 使用调度算法如轮转、优先级调度等

2. **应用级调度**：
    - 在用户空间管理任务执行顺序
    - 常见于协程、绿色线程等轻量级并发模型

3. **I/O 调度**：
    - 管理 I/O 操作的执行顺序
    - 影响并发程序的吞吐量和响应时间

### 调度策略

- **抢占式调度**：调度器可以中断当前任务（如操作系统线程调度）
- **协作式调度**：任务主动让出控制权（如协程）


