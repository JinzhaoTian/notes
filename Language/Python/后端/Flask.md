
## Jinja2

jinja2 是 Flask 作者开发的一个模板系统，起初是仿 django 模板的一个模板引擎，为 Flask 提供模板支持，由于其灵活，快速和安全等优点被广泛使用。




## Flask-RESTx

`Flask-RESTx` 是 `Flask` 框架的一个扩展库，用于简化构建基于 RESTful API 的应用。它是 `Flask-RESTPlus` 的继任者，主要解决了 `Flask-RESTPlus` 一些过时依赖的问题，同时提供了更好的文档化、序列化和 API 架构管理功能。