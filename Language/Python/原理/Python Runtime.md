





> [!tip] GIL 变更
> Python 委员会接受了 PEP 703，并计划分阶段进行谨慎的引入：
> 1. Python 3.13（于 2024 年发布）：可选的无 GIL 构建，用于测试和社区反馈（由 `--disable-gil` 构建时间标志控制）。
> 2. 过渡阶段（~2026-2027）：统一构建，实现 GIL 的运行时切换。
> 3. 未来目标（~2028+）：No-GIL Python 成为默认，完全解锁多核并行性。

