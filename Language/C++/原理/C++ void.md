`void *` 是 C/C++ 语言中一种非常基础且强大的工具，理解它对于掌握内存管理、通用编程和系统级编程至关重要。

`void` 在 C/C++ 语言中通常表示无类型，`void *` 可以理解为指向一个无类型数据的指针，可以存储任何数据类型的地址，并不直接知道其所指向的数据的类型和大小，**因此 `void *` 被定义为指向任何类型数据的指针（或通用指针、泛型指针）**。

## 主要特点

