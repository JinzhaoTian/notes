# 特征处理

lr、svm、knn等算法**只能处理数值型数据**，而不能处理文字，因此有时候我们需要对不同类型的特征进行预处理，转化为数值型数据。具体的特征类型有如下几种：

1. 字符串类型
2. 缺失值
3. 连续型特征
4. 归一化



## 字符串类型特征处理

字符串类型特征一般都是对它进行编码，字符的含义的不同，一般有如下几种形式：

1. 有序：分类变量的取值之间有大小意义时，如【小学、初中、大学】，可以使用分类编码，将其编码为【0,1,2】。
2. 无序：分类变量的取值之间没有大小意义时，如【蓝色、黄色、红色】，则使用独热编码。

