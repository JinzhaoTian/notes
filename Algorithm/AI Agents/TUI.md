TUI（Text-based User Interface，文本用户界面），其完全运行在终端或命令行窗口中，使用字符和符号来构建界面元素。

## 主要特点

- **基于文本**：整个界面由字母、数字、符号和线条组成，没有图片或复杂的图形。
- **键盘驱动**：几乎所有操作都通过键盘完成，例如使用方向键移动光标、按 Tab 键切换选项、按回车键确认等。
- **结构清晰**：虽然只有文本，但 TUI 会通过布局、颜色和高亮来组织信息，形成菜单、窗口、按钮等可视结构，比纯命令行更直观。

