
二分查找难得不是写，而是如何灵活的应用上面。二分查找算法一般要求**数组是有序的**，应用过程中必须要设置一个**基准值**，用数组中间部位的值与基准值比较进而更新查找区间。

```
int binary_search(int target, vector<int> nums){
    int left = 0, right = nums.size() - 1;
    while(left < right) {
        int mid = (left + right) / 2;
        if (nums[mid] == target) return mid;
        else if (nums[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1;
}
```

还有比较特别的用法如 `nums[mid] > target` 与 `nums[mid] >= target`的区别，这是边界的问题：

```
// 查找答案的左边界
int left_bound(int target, vector<int> nums){
    int left = 0, right = nums.size() - 1;
    while(left < right) {
        int mid = (left + right) / 2;
        if(target <= nums[mid]){
            right = mid;
        } else{
            left = mid+1;
        }
    }
    return left;
}

// 查找答案的右边界
int right_bound(int target, vector<int> nums){
    int left = 0, right = nums.size() - 1;
    while(left < right) {
        int mid = (left + right) / 2;
        if(target < nums[mid]){
            right=mid - 1;
        } else{
            left=mid;
        }
    }
    return right;
}
```

### 适用场景

1. 有序数组查找某个数
    1. [LeetCode 34. 在排序数组中查找元素的第一个和最后一个位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/)
    2. [LeetCode 287. 寻找重复数](https://leetcode.cn/problems/find-the-duplicate-number/)
2. 猜答案

这种思想还是挺奇怪的，总之就是先用二分查找猜一个答案，不符合就继续查找下去。这种方法可以加快猜的过程。

1. [LeetCode 69. x 的平方根](https://leetcode.cn/problems/sqrtx/)
2. [LeetCode 29. 两数相除](https://leetcode.cn/problems/divide-two-integers/)

3. 查找答案：
    1. [LeetCode 793. 阶乘函数后 K 个零](https://leetcode.cn/problems/preimage-size-of-factorial-zeroes-function/) 涉及到大范围10**9第K个最大最小，K个数量这种很多都是二分解法。